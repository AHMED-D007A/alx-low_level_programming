#include "main.h"
/**
*mul - calculate the multiplie of two numbers
*@a: the first number
*@b: the secong number
*Return: the multiplication
*/
int mul(int a, int b)
{
	int m;

	m = a * b;

	return (m);
}
